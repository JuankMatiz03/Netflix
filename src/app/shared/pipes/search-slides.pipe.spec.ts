import { SearchSlidesPipe } from './search-slides.pipe';

describe('SearchSlidesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSlidesPipe();
    expect(pipe).toBeTruthy();
  });
});
